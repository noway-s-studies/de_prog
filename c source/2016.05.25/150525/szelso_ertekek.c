#include <stdio.h>
#include <stdlib.h>

/*
int main() {
    int szamok;
    
    do{
        scanf("%d", &szamok);
        if(szamok){
            int i = 0, elso = 0, utolso = 0, szam = 0;
            for(i = 1; i <= szamok; i++){
                scanf(" %d", &szam);
                if(i == 1){
                    elso = szam;
                    
                }
                if(i == szamok){
                    utolso = szam;
                }
            }
            
            puts(elso == utolso ? "YES" : "NO");
        }
    }while(szamok);
    return (EXIT_SUCCESS);
}
*/
